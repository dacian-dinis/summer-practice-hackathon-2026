CX-31 — polish pass: longer onboarding + complete RO translations + style audit + mobile + user ranks

CONTEXT
This is ShowUp2Move (Next.js 14 App Router + Prisma SQLite, brand orange #FC5200, Archivo Black `font-display`, JetBrains Mono `font-mono-label`, sharp `rounded-md`, `border-2 border-brand-ink` accents, brand cream `bg-brand-cream`, deep blue accent `bg-bluebold` for AI surfaces, neutral neutrals).

The Prisma client is at `src/generated/prisma`. Sport list now includes Football, Tennis, Basketball, Padel, Volleyball, Jogging, Cycling, Running, Yoga, Hiking (see `src/lib/sports.ts` and `prisma/seed.ts`). i18n primitives at `src/lib/i18n.ts`, `src/components/locale-toggle.tsx`. Notifications at `src/lib/notifications.ts`.

Run `npm run db:seed` (after pushing your schema if you change it) — note the existing seed uses `prisma.user.upsert({where:{name}})` and runs idempotently. Do NOT add new prisma migrations or change the schema unless required (Scope D requires NO schema change — derive ranks from existing data).

DO NOT touch:
- `tailwind.config.ts`, `globals.css`, logo files, `prisma/schema.prisma`
- The Codex CLI brief files (`tmp_*.txt`)
- The `.claude/`, `.next/`, `src/generated/` directories.

After all scopes, run `npx tsc --noEmit` and report errors only. Final `git diff --stat` summary.

────────────────────────────────────────────────
SCOPE A — Longer onboarding (CURRENTLY 3 STEPS)

`src/app/onboarding/onboarding-client.tsx` and the step pages under `src/app/onboarding/{availability,bio,profile}` already exist. Add 2 more steps so the flow becomes 5 steps. The new steps (server components) must be:

A1. NEW route `src/app/onboarding/skill/page.tsx`:
   - Headline `font-display text-3xl/4xl` "How do you usually play?"
   - Description: "Help us match you with people of similar skill."
   - 5 selectable cards/buttons in a horizontal grid (1=Casual, 2=Hobby, 3=Regular, 4=Competitive, 5=Elite). Each card has an emoji + label + 1-line description. Use the existing UI primitives (Button, Card). Sharp corners, brand-ink borders, hover:border-brand.
   - The selected card must be visually emphasized (ring-2 ring-brand or bg-brand text-white).
   - Submit via existing pattern (server action). Read the existing onboarding step components to mirror style/server-action pattern. Persist to `User.skill` (1-5).
   - On success advance to next step.

A2. NEW route `src/app/onboarding/times/page.tsx`:
   - Headline "When do you usually play?"
   - Description: "We'll prioritize matches that line up with your schedule."
   - Multi-select chips: "Mornings (6–10)", "Lunch (11–14)", "Afternoons (15–17)", "Evenings (18–22)", "Weekends only".
   - Persist to `User.bio` by APPENDING a structured tag like `\n\n[playtimes: evenings,weekends]` (since we cannot add a column without a migration). Or — simpler — store in a NEW JSON column-free way: persist nothing; instead, create a server action that updates `User.bio` to APPEND the chosen times if not already present. If you find a cleaner approach without schema change (e.g., reuse `bio` text), use it.
   - Submit advances onboarding.

A3. Update the onboarding step indicator/breadcrumb (look in onboarding layout or onboarding-client) to show 5 dots/steps instead of 3.

A4. The flow order must become: profile → bio → skill → times → availability → done. Update redirect targets in each step's server action / "next" link accordingly.

A5. Use i18n keys for all visible copy. Add the keys to `src/lib/i18n.ts` (`onb.skill.heading`, `onb.skill.subhead`, `onb.skill.label1..5`, `onb.times.heading`, `onb.times.subhead`, `onb.times.morning`, `onb.times.lunch`, `onb.times.afternoon`, `onb.times.evening`, `onb.times.weekends`, plus EN + RO translations).

────────────────────────────────────────────────
SCOPE B — COMPLETE RO TRANSLATIONS

The dictionary in `src/lib/i18n.ts` covers landing, dashboard, share, nav. Many other surfaces still hard-code English. Add EN + RO entries for all visible strings on these surfaces (translate via `getDict()` server-side or pass labels as props for client components):

- `src/app/profile/page.tsx` and any sub-components: profile heading, "Your sports", "Save profile", "Detect sports from bio", "Detect sports from photo", "Skill level", "ShowUp Score" badge label, "Edit bio", upload photo button copy.
- `src/components/captain-copilot.tsx`: "AI Captain Copilot" button text, "Post poll to group", error toasts ("Could not generate copilot suggestions.", "Could not post the poll."), "Captain Copilot Picks", "Option N", "RON" suffix, "Suggested time", "Using fallback suggestions", "✨ Copilot ready".
- `src/app/groups/page.tsx`: "Your groups", empty-state copy, "View group" CTA, status labels.
- `src/app/groups/[id]/page.tsx`: "Group Detail" badge, "Captain {name}", "Members", "Confirmed"/"Waiting for confirmation", "Not in this group", any other literal copy.
- `src/components/event-card.tsx`: "Start time", "Open in Google Maps", "Add to calendar".
- `src/components/team-balance.tsx`: "Skill-balanced split", "Suggested teams", "Auto-balanced…", "Avg skill", "Team A"/"Team B", "Δ {n} skill points".
- `src/components/marketing-nav.tsx`: any nav labels.
- `src/components/notification-bell.tsx`: "Notifications", "Nothing yet…", "{n}h ago" — keep `formatRelative` numbers but translate the suffix words for RO ("h ago" → "ore în urmă", "m ago" → "min în urmă", "d ago" → "zile în urmă", "just now" → "acum").

For client components that need labels, accept a `labels` prop from a parent server component (the same pattern already used by `<Nav>`).

You can pick a sensible key naming scheme, e.g. `profile.title`, `copilot.button`, `group.members`, etc. EN values should match current literals; RO values must be natural Romanian (not Google-translate quality — concise + idiomatic).

────────────────────────────────────────────────
SCOPE C — STYLE AUDIT + MOBILE PASS

Audit these pages for visual consistency with the Bold style (sharp `rounded-md`, `border-2 border-brand-ink`, font-display headings, font-mono-label eyebrows) and for mobile readability at 375px:
- `/profile` and its sub-components
- `/groups` (list)
- `/events/new`
- `/map` (the page wrapper, not the leaflet component)

For each page:
1. Replace any `rounded-2xl`/`rounded-xl` outer cards with `rounded-md border-2 border-brand-ink dark:border-neutral-50 bg-white dark:bg-neutral-950 shadow-none`.
2. Add `font-display` to top-level page headings; add a `font-mono-label` eyebrow above headings (e.g., "PROFILE · YOUR PROFILE").
3. Ensure any primary CTA uses `bg-brand text-white hover:bg-brand-deep font-bold uppercase tracking-wider min-h-11 rounded-md`.
4. Mobile (`sm:` and below): remove fixed widths, ensure `flex-wrap` on toolbars, ensure `text-3xl` headings instead of `text-5xl` until `sm:` breakpoint.
5. `<MarketingNav>` and `<Nav>`: add a hamburger-menu collapse for the link list at <`sm` to avoid two-row wrap.

Do not redesign — refine the existing layouts. Keep functionality identical.

────────────────────────────────────────────────
SCOPE D — USER RANKS

D1. Add to `src/lib/ranks.ts`:
   ```ts
   export type Rank = { tier: string; emoji: string; min: number; color: string };
   export const RANKS: Rank[] = [
     { tier: "Rookie", emoji: "🌱", min: 0, color: "bg-neutral-100 text-neutral-700 dark:bg-neutral-800 dark:text-neutral-300" },
     { tier: "Regular", emoji: "🔥", min: 3, color: "bg-amber-100 text-amber-900 dark:bg-amber-950 dark:text-amber-300" },
     { tier: "Captain", emoji: "⭐", min: 8, color: "bg-emerald-100 text-emerald-800 dark:bg-emerald-950 dark:text-emerald-300" },
     { tier: "MVP", emoji: "🏆", min: 18, color: "bg-bluebold text-white" },
     { tier: "Legend", emoji: "👑", min: 35, color: "bg-brand text-white" },
   ];
   export function rankFor(confirmedEventsCount: number): Rank { ... }
   ```

D2. Compute `confirmedEventsCount` for a user as:
   `count of events where the user is a GroupMember with confirmed=true and the event.startsAt < now()`.
   (i.e., past events they confirmed.)

D3. Render rank badge:
   - On `/profile`: show as a hero badge near the user name, plus a "next rank in N games" hint.
   - On `/groups/[id]`: next to each member's name, render a small chip with rank tier emoji + name (override the existing "Confirmed/Waiting" line to be a row with rank + status).

D4. Add i18n keys for rank tier labels (`rank.rookie`, `rank.regular`, etc. + EN + RO).

────────────────────────────────────────────────
DELIVERABLES

1. `git diff --stat` summary.
2. List of new files vs modified, grouped by scope (A/B/C/D).
3. Output of `npx tsc --noEmit` (errors only).
4. Brief commentary on any deviation from the brief and why.

CONSTRAINTS
- TS strict, single quotes/double quotes consistent with each file's existing style.
- All async fetches go to existing API contracts — don't introduce new APIs unless strictly needed.
- Keep the Codex CLI brief idempotent: run twice = same result.
- If you change a server action signature, update all callers.
- After D, dashboard and group page must keep working (verify the join-button / member rendering still compiles).
- Use existing patterns; do not add new dependencies.
